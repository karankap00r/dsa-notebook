package sort

/*
	- bubble sort
	- selection sort
	- insertion sort
	- quick sort
	- merge sort
	- bucket sort
	- radix sort
	- find max adjacent element gap after sorting in an array in O(n) time
	- find majority element ie the one that occurs > n/2 times
	- find majority element ie the one that occurs > n/3 times
	- search for an element in row-wise and column-wise sorted (in ascending order) matrix
*/
type QuickSort struct{}

func (qs QuickSort) Sort(arr []int) {

}
